global:
  message: Customer service prod environment
server:
  port: 8083
message: Hello from prod environment
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_prod
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
