global:
  message: Microservices environment
logging:
  level:
    root: info
management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh
  endpoint:
    health:
      show-details: always